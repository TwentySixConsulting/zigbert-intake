# Same Supabase project as the waitlist. The anon key is safe in a public build:
# row-level security allows INSERT only, so it cannot read anyone's submission back.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
